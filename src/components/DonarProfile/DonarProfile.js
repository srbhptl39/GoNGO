import React, { Component } from 'react';
import { Button } from 'react-bootstrap';
import history from '../../history';
class DonarProfile extends Component {
    render() {
        return (
            <div>
                hello
            </div>
        );
    }
}

export default DonarProfile;